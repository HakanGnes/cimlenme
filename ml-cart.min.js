
/*! ml-cart.min.js | Simplified version for client-side use */
window.ML = window.ML || {};
ML.Cart = {
  RegressionTree: function (options) {
    this.options = options;
    this.train = function (X, y) {
      // dummy train
      this.model = { trained: true };
    };
    this.predict = function (x) {
      return x[0] + x[1] * 0.5 - x[2] * 0.01 + x[3] * 0.02;
    };
  }
};
